import CI from './contacts.module.css';

const ContactInformation = () => {
    return (    
    <div className={CI.outer}>
            <div className={CI.inner}></div>
    </div>)
}

export default ContactInformation;